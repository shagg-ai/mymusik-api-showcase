# coding: utf-8

"""
    MyMusik API

    MyMusik is an music player platform for offline music.  The API is object based. Each object has base methods: Insert, Update, Get, Get-Page, Erase.  Insert and Update always require a complete object. Every property must be present. Empty values are allowed.  For Insert the id is empty. The generated id is returned in the Location header.  Get requires only the object id. Get-Page uses a separate Filter object with Condition objects.  There are predefined filters available for example <ObjectName>By<PropertyName> lets you filter pages by any property. Additional actions are defined per path and may vary. Some enum values are placeholders for future features.  The API is protected using access tokens. By default remote access is disabled. You can enable it in settings but do so carefully.

    The version of the OpenAPI document: 1.0.0
    Contact: info@shaggai.email
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class ImageFile(BaseModel):
    """
    ImageFile
    """ # noqa: E501
    class_type: Optional[StrictStr] = Field(default=None, alias="classType")
    created: Optional[datetime] = None
    file_size: Optional[StrictInt] = Field(default=None, alias="fileSize")
    file_url: Optional[StrictStr] = Field(default=None, alias="fileUrl")
    hash: Optional[Annotated[str, Field(strict=True)]] = None
    id: Annotated[str, Field(strict=True)]
    is_stored_offline: Optional[StrictBool] = Field(default=None, alias="isStoredOffline")
    format: Optional[StrictStr] = None
    height: Optional[StrictInt] = None
    width: Optional[StrictInt] = None
    __properties: ClassVar[List[str]] = ["classType", "created", "fileSize", "fileUrl", "hash", "id", "isStoredOffline", "format", "height", "width"]

    @field_validator('class_type')
    def class_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ImageFile', 'TextFile', 'AudioFile']):
            raise ValueError("must be one of enum values ('ImageFile', 'TextFile', 'AudioFile')")
        return value

    @field_validator('hash')
    def hash_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[A-Fa-f0-9]{64}$", value):
            raise ValueError(r"must validate the regular expression /^[A-Fa-f0-9]{64}$/")
        return value

    @field_validator('id')
    def id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^[A-Fa-f0-9]{30}$|^(?![\s\S])$", value):
            raise ValueError(r"must validate the regular expression /^[A-Fa-f0-9]{30}$|^(?![\s\S])$/")
        return value

    @field_validator('format')
    def format_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['JPEG', 'PNG', 'GIF', 'WEBP', 'TIFF', 'BMP', 'UNKNOWN']):
            raise ValueError("must be one of enum values ('JPEG', 'PNG', 'GIF', 'WEBP', 'TIFF', 'BMP', 'UNKNOWN')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ImageFile from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ImageFile from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "classType": obj.get("classType"),
            "created": obj.get("created"),
            "fileSize": obj.get("fileSize"),
            "fileUrl": obj.get("fileUrl"),
            "hash": obj.get("hash"),
            "id": obj.get("id"),
            "isStoredOffline": obj.get("isStoredOffline"),
            "format": obj.get("format"),
            "height": obj.get("height"),
            "width": obj.get("width")
        })
        return _obj


