/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ArtistMetrics } from '../../models/artist-metrics';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceArtistMetricsGetPage$Params {

/**
 * the name of a Filter resource
 */
  filterName?: string;

/**
 * input parameter for the filter use ';' to separate values
 */
  values?: string;
  sortBy?: 'playDuration' | 'totalSongPlays' | 'skipCount' | 'skipRate' | 'lastPlayed' | 'firstPlayed' | 'month';
  orderBy?: 'ASC' | 'DESC' | 'RANDOM';
  pageIndex?: number;
}

export function serviceArtistMetricsGetPage(http: HttpClient, rootUrl: string, params?: ServiceArtistMetricsGetPage$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ArtistMetrics>>> {
  const rb = new RequestBuilder(rootUrl, serviceArtistMetricsGetPage.PATH, 'get');
  if (params) {
    rb.query('filterName', params.filterName, {});
    rb.query('values', params.values, {});
    rb.query('sortBy', params.sortBy, {});
    rb.query('orderBy', params.orderBy, {});
    rb.query('pageIndex', params.pageIndex, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ArtistMetrics>>;
    })
  );
}

serviceArtistMetricsGetPage.PATH = '/artistmetrics';

export { serviceArtistMetricsGetPage as getPage };
