/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Synchronisation } from '../../models/synchronisation';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceSynchronisationGet$Params {

/**
 * The identifier of a resource
 */
  id: IdentifierLike;
}

export function serviceSynchronisationGet(http: HttpClient, rootUrl: string, params: ServiceSynchronisationGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Synchronisation>> {
  const rb = new RequestBuilder(rootUrl, serviceSynchronisationGet.PATH, 'get');
  if (params) {
    rb.path('id', String(params.id), {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Synchronisation>;
    })
  );
}

serviceSynchronisationGet.PATH = '/synchronisation/{id}';

export { serviceSynchronisationGet as get };
