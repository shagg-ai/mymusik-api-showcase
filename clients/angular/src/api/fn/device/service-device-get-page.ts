/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Device } from '../../models/device';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceDeviceGetPage$Params {

/**
 * the name of a Filter resource
 */
  filterName?: string;

/**
 * input parameter for the filter use ';' to separate values
 */
  values?: string;
  sortBy?: 'operatingSystem' | 'ip' | 'location' | 'osVersion' | 'processor' | 'appVersion' | 'apiVersion' | 'appInstallDate' | 'lastAppUpdate' | 'lastOnlineCheck' | 'storageUsage' | 'maxStorage' | 'isTrusted' | 'isInitialized' | 'isMobile' | 'hasDarkModeOn' | 'loggedIn' | 'lastLogin' | 'cloudLastServerRevision';
  orderBy?: 'ASC' | 'DESC' | 'RANDOM';
  pageIndex?: number;
}

export function serviceDeviceGetPage(http: HttpClient, rootUrl: string, params?: ServiceDeviceGetPage$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<Device>>> {
  const rb = new RequestBuilder(rootUrl, serviceDeviceGetPage.PATH, 'get');
  if (params) {
    rb.query('filterName', params.filterName, {});
    rb.query('values', params.values, {});
    rb.query('sortBy', params.sortBy, {});
    rb.query('orderBy', params.orderBy, {});
    rb.query('pageIndex', params.pageIndex, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<Device>>;
    })
  );
}

serviceDeviceGetPage.PATH = '/device';

export { serviceDeviceGetPage as getPage };
