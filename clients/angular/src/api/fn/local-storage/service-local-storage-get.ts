/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LocalStorage } from '../../models/local-storage';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceLocalStorageGet$Params {

/**
 * The identifier of a resource
 */
  id: IdentifierLike;
}

export function serviceLocalStorageGet(http: HttpClient, rootUrl: string, params: ServiceLocalStorageGet$Params, context?: HttpContext): Observable<StrictHttpResponse<LocalStorage>> {
  const rb = new RequestBuilder(rootUrl, serviceLocalStorageGet.PATH, 'get');
  if (params) {
    rb.path('id', String(params.id), {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LocalStorage>;
    })
  );
}

serviceLocalStorageGet.PATH = '/localstorage/{id}';

export { serviceLocalStorageGet as get };
