/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Album } from '../../models/album';
import { Artist } from '../../models/artist';
import { Playlist } from '../../models/playlist';
import { Song } from '../../models/song';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceMusicElementUpdate$Params {

/**
 * The identifier of a resource
 */
  id: IdentifierLike;
  
    /**
     * MusicElement that should be updated
     */
    body?: (Song | Album | Artist | Playlist)
}

export function serviceMusicElementUpdate(http: HttpClient, rootUrl: string, params: ServiceMusicElementUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, serviceMusicElementUpdate.PATH, 'put');
  if (params) {
    rb.path('id', String(params.id), {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

serviceMusicElementUpdate.PATH = '/musicelement/{id}';

export { serviceMusicElementUpdate as update };
