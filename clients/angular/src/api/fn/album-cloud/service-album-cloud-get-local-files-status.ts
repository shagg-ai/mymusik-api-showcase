/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FileIdentifierList } from '../../models/file-identifier-list';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceAlbumCloudGetLocalFilesStatus$Params {

/**
 * The identifier of a resource
 */
  id: IdentifierLike;
}

export function serviceAlbumCloudGetLocalFilesStatus(http: HttpClient, rootUrl: string, params: ServiceAlbumCloudGetLocalFilesStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<FileIdentifierList>> {
  const rb = new RequestBuilder(rootUrl, serviceAlbumCloudGetLocalFilesStatus.PATH, 'get');
  if (params) {
    rb.path('id', String(params.id), {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FileIdentifierList>;
    })
  );
}

serviceAlbumCloudGetLocalFilesStatus.PATH = '/albumcloud/{id}/getlocalfilesstatus';

export { serviceAlbumCloudGetLocalFilesStatus as getLocalFilesStatus };
