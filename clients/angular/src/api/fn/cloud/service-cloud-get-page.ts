/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Cloud } from '../../models/cloud';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceCloudGetPage$Params {

/**
 * the name of a Filter resource
 */
  filterName?: string;

/**
 * input parameter for the filter use ';' to separate values
 */
  values?: string;
  sortBy?: 'fileUrl' | 'fileSize' | 'compressedSize' | 'description' | 'sig' | 'type' | 'status' | 'modus' | 'cloudLinkId' | 'ownerId' | 'lastUpdate' | 'created' | 'expires' | 'lastOnlineCheck' | 'lastDownload' | 'shouldExpire' | 'shouldRemove' | 'revision' | 'isDeleted' | 'deletedAt';
  orderBy?: 'ASC' | 'DESC' | 'RANDOM';
  pageIndex?: number;
}

export function serviceCloudGetPage(http: HttpClient, rootUrl: string, params?: ServiceCloudGetPage$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<Cloud>>> {
  const rb = new RequestBuilder(rootUrl, serviceCloudGetPage.PATH, 'get');
  if (params) {
    rb.query('filterName', params.filterName, {});
    rb.query('values', params.values, {});
    rb.query('sortBy', params.sortBy, {});
    rb.query('orderBy', params.orderBy, {});
    rb.query('pageIndex', params.pageIndex, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<Cloud>>;
    })
  );
}

serviceCloudGetPage.PATH = '/cloud';

export { serviceCloudGetPage as getPage };
