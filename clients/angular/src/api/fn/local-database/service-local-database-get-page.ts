/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DatabaseBackup } from '../../models/database-backup';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceLocalDatabaseGetPage$Params {

/**
 * the name of a Filter resource
 */
  filterName?: string;

/**
 * input parameter for the filter use ';' to separate values
 */
  values?: string;
  sortBy?: 'version' | 'fileSize' | 'compressedSize' | 'cloudId' | 'isLocked' | 'fileCreated' | 'fileLastModified' | 'locked' | 'created' | 'finished' | 'lastCheck';
  orderBy?: 'ASC' | 'DESC' | 'RANDOM';
  pageIndex?: number;
}

export function serviceLocalDatabaseGetPage(http: HttpClient, rootUrl: string, params?: ServiceLocalDatabaseGetPage$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<DatabaseBackup>>> {
  const rb = new RequestBuilder(rootUrl, serviceLocalDatabaseGetPage.PATH, 'get');
  if (params) {
    rb.query('filterName', params.filterName, {});
    rb.query('values', params.values, {});
    rb.query('sortBy', params.sortBy, {});
    rb.query('orderBy', params.orderBy, {});
    rb.query('pageIndex', params.pageIndex, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<DatabaseBackup>>;
    })
  );
}

serviceLocalDatabaseGetPage.PATH = '/localdatabase';

export { serviceLocalDatabaseGetPage as getPage };
