/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AlbumIdentifier } from '../../models/album-identifier';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceAlbumIdentifierGet$Params {

/**
 * The identifier of a resource
 */
  id: IdentifierLike;
}

export function serviceAlbumIdentifierGet(http: HttpClient, rootUrl: string, params: ServiceAlbumIdentifierGet$Params, context?: HttpContext): Observable<StrictHttpResponse<AlbumIdentifier>> {
  const rb = new RequestBuilder(rootUrl, serviceAlbumIdentifierGet.PATH, 'get');
  if (params) {
    rb.path('id', String(params.id), {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AlbumIdentifier>;
    })
  );
}

serviceAlbumIdentifierGet.PATH = '/albumidentifier/{id}';

export { serviceAlbumIdentifierGet as get };
