/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SongList } from '../../models/song-list';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceAlbumGetSongsNotAvailable$Params {

/**
 * The identifier of a resource
 */
  id: IdentifierLike;
}

export function serviceAlbumGetSongsNotAvailable(http: HttpClient, rootUrl: string, params: ServiceAlbumGetSongsNotAvailable$Params, context?: HttpContext): Observable<StrictHttpResponse<SongList>> {
  const rb = new RequestBuilder(rootUrl, serviceAlbumGetSongsNotAvailable.PATH, 'get');
  if (params) {
    rb.path('id', String(params.id), {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SongList>;
    })
  );
}

serviceAlbumGetSongsNotAvailable.PATH = '/album/{id}/getsongsnotavailable';

export { serviceAlbumGetSongsNotAvailable as getSongsNotAvailable };
