/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AudioImport } from '../../models/audio-import';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceAudioImportUpdate$Params {

/**
 * The identifier of a resource
 */
  id: IdentifierLike;
  
    /**
     * AudioImport that should be updated
     */
    body?: AudioImport
}

export function serviceAudioImportUpdate(http: HttpClient, rootUrl: string, params: ServiceAudioImportUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, serviceAudioImportUpdate.PATH, 'put');
  if (params) {
    rb.path('id', String(params.id), {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

serviceAudioImportUpdate.PATH = '/audioimport/{id}';

export { serviceAudioImportUpdate as update };
