/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ImageUrlImport } from '../../models/image-url-import';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceImageUrlImportGet$Params {

/**
 * The identifier of a resource
 */
  id: IdentifierLike;
}

export function serviceImageUrlImportGet(http: HttpClient, rootUrl: string, params: ServiceImageUrlImportGet$Params, context?: HttpContext): Observable<StrictHttpResponse<ImageUrlImport>> {
  const rb = new RequestBuilder(rootUrl, serviceImageUrlImportGet.PATH, 'get');
  if (params) {
    rb.path('id', String(params.id), {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ImageUrlImport>;
    })
  );
}

serviceImageUrlImportGet.PATH = '/imageurlimport/{id}';

export { serviceImageUrlImportGet as get };
