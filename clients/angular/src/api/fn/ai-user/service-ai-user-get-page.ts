/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AiUser } from '../../models/ai-user';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceAiUserGetPage$Params {

/**
 * the name of a Filter resource
 */
  filterName?: string;

/**
 * input parameter for the filter use ';' to separate values
 */
  values?: string;
  sortBy?: 'plan' | 'tokens' | 'topupTokens' | 'tokensUsedThisMonth' | 'tokensUsedMonth' | 'tokensUsedTotal' | 'tokensToAddMonthly' | 'created' | 'lastAccess' | 'lastTokenUpdate' | 'subscriptionEnd' | 'referralCode' | 'membershipLevel' | 'purchasedPacks' | 'isYearlyPayment' | 'isBanned';
  orderBy?: 'ASC' | 'DESC' | 'RANDOM';
  pageIndex?: number;
}

export function serviceAiUserGetPage(http: HttpClient, rootUrl: string, params?: ServiceAiUserGetPage$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<AiUser>>> {
  const rb = new RequestBuilder(rootUrl, serviceAiUserGetPage.PATH, 'get');
  if (params) {
    rb.query('filterName', params.filterName, {});
    rb.query('values', params.values, {});
    rb.query('sortBy', params.sortBy, {});
    rb.query('orderBy', params.orderBy, {});
    rb.query('pageIndex', params.pageIndex, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<AiUser>>;
    })
  );
}

serviceAiUserGetPage.PATH = '/aiuser';

export { serviceAiUserGetPage as getPage };
