/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ClassTypeCondition } from '../../models/class-type-condition';
import { DateTimeCondition } from '../../models/date-time-condition';
import { NumberCondition } from '../../models/number-condition';
import { StringCondition } from '../../models/string-condition';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceConditionGet$Params {

/**
 * The identifier of a resource
 */
  id: IdentifierLike;
}

export function serviceConditionGet(http: HttpClient, rootUrl: string, params: ServiceConditionGet$Params, context?: HttpContext): Observable<StrictHttpResponse<(NumberCondition | StringCondition | ClassTypeCondition | DateTimeCondition)>> {
  const rb = new RequestBuilder(rootUrl, serviceConditionGet.PATH, 'get');
  if (params) {
    rb.path('id', String(params.id), {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<(NumberCondition | StringCondition | ClassTypeCondition | DateTimeCondition)>;
    })
  );
}

serviceConditionGet.PATH = '/condition/{id}';

export { serviceConditionGet as get };
