/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ClassTypeCondition } from '../../models/class-type-condition';
import { DateTimeCondition } from '../../models/date-time-condition';
import { NumberCondition } from '../../models/number-condition';
import { StringCondition } from '../../models/string-condition';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceConditionUpdate$Params {

/**
 * The identifier of a resource
 */
  id: IdentifierLike;
  
    /**
     * Condition that should be updated
     */
    body?: (NumberCondition | StringCondition | ClassTypeCondition | DateTimeCondition)
}

export function serviceConditionUpdate(http: HttpClient, rootUrl: string, params: ServiceConditionUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, serviceConditionUpdate.PATH, 'put');
  if (params) {
    rb.path('id', String(params.id), {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

serviceConditionUpdate.PATH = '/condition/{id}';

export { serviceConditionUpdate as update };
