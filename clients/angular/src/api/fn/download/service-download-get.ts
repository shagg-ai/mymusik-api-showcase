/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Download } from '../../models/download';
import { Identifier } from '../../models/Identifier';
export type IdentifierLike = Identifier | string;



export interface ServiceDownloadGet$Params {

/**
 * The identifier of a resource
 */
  id: IdentifierLike;
}

export function serviceDownloadGet(http: HttpClient, rootUrl: string, params: ServiceDownloadGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Download>> {
  const rb = new RequestBuilder(rootUrl, serviceDownloadGet.PATH, 'get');
  if (params) {
    rb.path('id', String(params.id), {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Download>;
    })
  );
}

serviceDownloadGet.PATH = '/download/{id}';

export { serviceDownloadGet as get };
